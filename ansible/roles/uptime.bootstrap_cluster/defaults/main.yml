local_kubeconfig_path: ~/.kube/guac3s.kubeconfig

cilium_chart_version: 1.13.0

ingress_nginx_chart_version: 4.15.1

ingress_nginx_chart_values: {}
# @alban: removed on 2026-09-19, "replicats: 2" was a typo for
# controller.replicaCount and was silently ignored by Helm (unknown
# key), so it never actually ran with 2 replicas. Chart default is
# controller.replicaCount: 1, which is what we've effectively always
# gotten -- left explicit here since that's fine.

certmanager_chart_version: 1.21.2

certmanager_chart_values:
  installCRDs: true

argocd_chart_version: 5.19.7 # argocd chart values: see role template

guacamole_release_name: guacamole

# guac_path: /guacamole-docker-compose
# guacamole_domain: guacamole.example.com
# guac_letsencrypt_email: eliegavoty@free.fr
guac_stagiaire_passwd: devops101
guac_formateur_passwd: devops202
base_unix_user: guacamole
